<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CheckOut Page</title>
  <link rel="stylesheet" href="./css/StyleCheckOut.css">
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="/vendor.css">
</head>
<body>
  <div class="mainscreen">
    <div class="card">
      <div class="leftside">
        <img src="data:<%= blog.img.contentType %>;base64,<%= blog.img.data.toString('base64') %>" class="card-img" alt="...">
      </div>
      <div class="rightside">
        <h1><%= blog.title %></h1>
        <h2><%= blog.author %></h2>
        <p><%- blog.introduction %></p>
        <p>Price: $<%= blog.price %></p>
        <form action="/user/buy-premium/<%= blog._id %>" method="POST">
           <button type="submit" class="button">Buy Premium Blog</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>